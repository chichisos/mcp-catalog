# Used for sitemap URLs and canonical tags. Change to your real domain.
NEXT_PUBLIC_SITE_URL=https://example.com

# Path to the source CSV (absolute). Build script reads from here.
CSV_PATH=/home/user/mcp_skills.csv

# How many top skills get pre-rendered on build (rest use ISR).
TOP_N_SSG=500
