/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ISR works by default with App Router. No special config needed.
  // metadataBase is set via env var (see app/layout.jsx).
}

export default nextConfig
